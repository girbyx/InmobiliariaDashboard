@inherits EnterpriseBalancesBase

<div class="text-center alert alert-secondary mt-4">
    <h3>Balances Generales</h3>
</div>
<div class="row">
    @if (Records != null && Records.Any())
    {
        foreach (var item in Records)
        {
            <div class="col">123</div>
        }
    }
</div>