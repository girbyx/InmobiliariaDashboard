@page "/costtype/list"
@inherits ListBase

<h1>Tipos de costos</h1>

<div class="text-right">
    <a href="costtype/add" class="btn btn-primary"><span class="oi oi-plus"></span> Agregar tipo de costo</a>
</div>

@if (Records == null)
{
    <p>
        <em>Cargando...</em>
    </p>
}
else
{
    <div class="table-responsive">
        <table class="table table-hover">
            <thead>
            <tr>
                <th>Tipo de costo</th>
                <th>Descripcion</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var record in Records)
            {
                <tr>
                    <td>@record.Name</td>
                    <td>@record.Description</td>
                    <td>
                        <a href="costtype/edit/@record.Id">
                            <span class="oi oi-pencil"></span>
                        </a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
}