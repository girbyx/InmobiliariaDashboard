@page "/projectsubtype/list"
@inherits ListBase

<h1>Sub-tipos de proyectos</h1>

<div class="text-right">
    <a href="projectsubtype/add" class="btn btn-primary"><span class="oi oi-plus"></span> Agregar sub-tipo de proyecto</a>
</div>

@if (Records == null)
{
    <p>
        <em>Cargando...</em>
    </p>
}
else
{
    <div class="table-responsive">
        <table class="table table-hover">
            <thead>
            <tr>
                <th>Sub-tipo de proyecto</th>
                <th>Descripcion</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var record in Records)
            {
                <tr>
                    <td>@record.Name</td>
                    <td>@record.Description</td>
                    <td>
                        <a href="projectsubtype/edit/@record.Id">
                            <span class="oi oi-pencil"></span>
                        </a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
}